docker debug exited containter
```
docker run --rm -it <img_id> /bin/bash
```

docker build cmds:
```
docker build -t .
```

docker images
```
docker rmi <img_id>
```
docker containers
```
docker rm <cntr_id>
```
